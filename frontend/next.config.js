/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  output: 'standalone',
  // Remove or comment out any export configuration
};

module.exports = nextConfig;
